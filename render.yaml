services:
  - type: web
    name: scoring-credit-api
    runtime: python
    buildCommand: pip install -r api/requirements.txt
    startCommand: gunicorn api.app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0